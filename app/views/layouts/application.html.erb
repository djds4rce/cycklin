<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= content_for?(:title) ? yield(:title) : "CyklIn" %></title>
<%= csrf_meta_tags %>

<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
<!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

<%= stylesheet_link_tag "application", :media => "all" %>

</head>
<body>

<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <div class="container nav-collapse">
        <a class="brand" href="#"><i class="icon-cogs"></i> CyklIn</a>
        <ul class='nav'>
          <li class='selected'> <a href='#'>Home</a></li>
          <li> 
          <a href='#'>About</a></li>
          <li> <a href='#'>Contact</a></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container-fluid">
  <div class="sidebar-nav">
    <h4> Search</h4>
    <div class='row'>
      <div class='well span2'>
        <h5>Brands</h5>
        <ul class='unstyled'>
          <% @brands.each do |brand| %>
            <li><label class="btn btn-primary"> <%= check_box_tag(brand[0].to_sym,brand[0],checkbox_type(params[:search],"brand",brand[0]),:class=>'search-tag brands') %> <%="#{brand[0].titleize}(#{brand[1]})" %></label> </li>
        <% end %>
        </ul>
      </div>
      <div class='well span2'>
        <h5>Type</h5>
        <ul class='unstyled'> 
          <% @types.each do |type| %>
            <li><label class="btn btn-primary"> <%= check_box_tag(type[0].to_sym,type[0],checkbox_type(params[:search],"type",type[0]),:class=>'search-tag types') %> <%="#{type[0]}(#{type[1]})" %></label> </li>
        <% end %>
        </ul>
      </div>  
      <div class='well span2'>
        <h5> Price</h5>

        <div class="noUiSlider"></div>
        <span class='range'></span>
      </div>  
    </div>
    </div><!--/span-->

  <div class="row main-row">
      <%= yield %>
  </div><!--/row-->
  <div class='footer-div'>
    <footer class='page-footer'>
      <p>&copy; Daya 2012. Images and data belong to respective parent sites.</p>
    </footer>
  </div>

</div> <!-- /container -->

<!-- Javascripts
    ================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<%= javascript_include_tag "application" %>

</body>
</html>
